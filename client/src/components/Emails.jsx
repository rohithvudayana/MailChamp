

const Emails = ({ openDrawer }) => {
    return (
        <div style= { openDrawer ? { marginLeft: 200, width: '100%'} : { width: '100%'}}>
             hello 
        </div>
    )
}

export default Emails